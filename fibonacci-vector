#include <iostream>
using namespace std;

int main()
{
	int t;
	int n;
	long long int fib[65];

	cin >> t;

	fib[0] = 0;
	fib[1] = 1;

	while(t--)
	{

		cin >> n;
		cout << "Fib(" << n << ") = ";


		for(int i = 2; i <= n; i++)
		{
			fib[i] = fib[i - 2] + fib[i - 1];
		}
		
		cout << fib[n] << endl;
	}



	return 0;
}
